{
  "permissions": {
    "allow": [
      "mcp__Metricoll__createScheduledPost",
      "mcp__Metricoll__getScheduledPosts",
      "mcp__Metricoll__updateScheduledPost",
      "mcp__Metricoll__getBrandSettings",
      "mcp__Metricoll__getAnalyticsDataByMetrics",
      "mcp__Metricoll__getBestTimeToPostByNetwork"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport sys, json\ntry:\n    obj = json.loads(sys.stdin.read())\n    prompt = obj.get('prompt', '')\nexcept:\n    prompt = ''\n\nkeywords = ['hook', 'хук', 'рилс', 'reel', 'aug', 'авг', 'контент', 'content', 'publish', 'публик', 'schedule', 'расписан', 'preview', 'превью', 'render', 'рендер', 'caption', 'капшен', 'clip', 'клип', 'footage', 'футаж', 'batch', 'батч', 'тема', 'topic', 'нища', 'niche', 'заголовок', 'title']\nif any(k in prompt.lower() for k in keywords):\n    msg = '⛔ KIRA PIPELINE — ОБЯЗАТЕЛЬНО:\\n0. footage-manager → выбери клип (не повтор!)\\n1. niche-templates → возьми шаблон по нише\\n2. /kira-hooks → аудит команды: Jordan · Mike · Alex · Sam · Dana · Red · Tyler\\n3. Mike 🟢 STRONG min. Red ✅ SURVIVED. Все 7 ✅ → рендер\\n4. /quality-check → 7-пунктовый QA gate\\n5. /metricool-ready → push + запланировать\\n6. performance-tracker → записать рилс\\n\\nБез аудита команды — хуки не выходят. Никогда.'\n    print(json.dumps({'hookSpecificOutput': {'hookEventName': 'UserPromptSubmit', 'additionalContext': msg}}))\nelse:\n    print(json.dumps({}))\n\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
