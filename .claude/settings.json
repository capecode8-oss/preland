{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport sys, json\ntry:\n    obj = json.loads(sys.stdin.read())\n    prompt = obj.get('prompt', '')\nexcept:\n    prompt = ''\n\nkeywords = ['hook', 'хук', 'рилс', 'reel', 'aug', 'авг', 'контент', 'content', 'publish', 'публик', 'schedule', 'расписан', 'preview', 'превью', 'render', 'рендер', 'caption', 'капшен', 'clip', 'клип', 'footage', 'футаж']\nif any(k in prompt.lower() for k in keywords):\n    msg = '⛔ KIRA PIPELINE — ОБЯЗАТЕЛЬНО:\\n1. Вызови /kira-hooks\\n2. Прогони через команду: Jordan · Mike · Alex · Sam · Dana · Red · Tyler\\n3. Mike ставит прогноз (минимум 🟢 STRONG). Red пытается убить хук (должен ✅ SURVIVED)\\n4. Все 7 зелёных → только тогда рендер/превью\\n\\nБез видимого аудита команды — хуки не выходят. Никогда.'\n    print(json.dumps({'hookSpecificOutput': {'hookEventName': 'UserPromptSubmit', 'additionalContext': msg}}))\nelse:\n    print(json.dumps({}))\n\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
